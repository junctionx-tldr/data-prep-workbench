{
  "name": "tldr-map-data",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build": "tsc --watch || true",
    "buildOnce": "tsc",
    "lint": "tslint --format stylish --project .",
    "prettier": "prettier --config node_modules/tslint-lt/.prettierrc.yml --write \"**/*.{ts,tsx,css,scss,html,js,jsx,yml,json,md}\" \"!{dist}/**\"",
    "test": "npm run buildOnce && npm run lint && npm run prettier",
    "prepare": "npm run buildOnce"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index",
  "types": "dist/index",
  "repository": {
    "type": "git",
    "url": "https://github.com/ltetzlaff/tldr-map-data.git"
  },
  "author": "ltetzlaff",
  "license": "All rights reserved.",
  "dependencies": {
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "tslint-lt": "^1.10.7",
    "typescript": "^3.6.4"
  }
}
